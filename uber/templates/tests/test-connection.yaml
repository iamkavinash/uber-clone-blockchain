apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "uber.fullname" . }}-test-connection"
  labels:
    {{- include "uber.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "uber.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
