apiVersion: v1
kind: Service
metadata:
  name: {{ include "superuber.fullname" . }}
  labels:
    {{- include "superuber.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "superuber.selectorLabels" . | nindent 4 }}
